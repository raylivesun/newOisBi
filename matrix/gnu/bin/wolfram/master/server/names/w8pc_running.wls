RealDigits[i] = digit;
RealDigits[i, b] = digit;
RealDigits[i, jj] = digit;
RealDigits[i, jj, len] = digit;
RealDigits[i, jj, len, local] = digit;
FromDigits[i, jj, len, local] = digit;
FromDigits[i, jj, len, local, "matrix"] = matrix;
FromDigits[i, jj, len, local, "lives"] = matrix;
FromDigits[i, jj, len, local, "networks"] = matrix;
FromDigits[i, jj, len, local, "connect"] = matrix;
FromDigits[i, jj, len, local, "token"] = token;
Done[z] = true;

